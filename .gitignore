# Rust build artifacts
/target/

# Generated install config — carries credentials and the IBM entitlement key.
# NEVER commit a populated copy (see WORKTREE.md / AC10).
cpd_vars.sh

# Logs
*.log

# Cargo lock is committed for the binary (reproducible builds); nothing to ignore.

# Added by BuildPilot installer
.buildpilot/state/
.buildpilot/tickets/
.worktree/
.buildpilot/lib
.buildpilot/tools
.buildpilot/providers
.claude/agents
.claude/skills

# Agent worktrees (isolated subagent sandboxes)
.claude/worktrees/

# cpd-cli olm-utils workspace (created at runtime)
cpd-cli-workspace/
